import React from "react";

// parent component controls total poem lines
// middle of component should be

function PoemBuilder() {

    let lines = []; // Array of strings representing the lines of a poem

    return(
        <div>

        </div>
    )
}

export default PoemBuilder;